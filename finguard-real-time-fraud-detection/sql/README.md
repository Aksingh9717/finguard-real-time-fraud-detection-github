# SQL and Reference Data

The SQL folder contains the PostgreSQL customer setup/incremental-load examples and a sanitized fraud-watchlist sample.

The files are for local/demo use. Replace all placeholder emails/card values with synthetic values in your own environment.
